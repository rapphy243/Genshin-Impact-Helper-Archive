version: "3"

services:
  genshin-impact-helper:
        build:
            context: ./
            dockerfile: ./Dockerfile
        image: rapphy243/genshin-helper #Tag conainer
        container_name: genshin-helper #Name container name
        environment:
            OS_COOKIE:
            DISCORD_WEBHOOK: